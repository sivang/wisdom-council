# wisdom-council - AWS CDK Project
# Generated by: bedsheet generate --target aws

[project]
name = "wisdom-council"
version = "0.1.0"
description = "A Bedsheet agent deployment on AWS"
requires-python = ">=3.11"
dependencies = [
    # AWS CDK
    "aws-cdk-lib>=2.100.0",
    "constructs>=10.0.0",
    # Bedsheet (for local testing)
    "bedsheet-agents>=0.4.0rc1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"